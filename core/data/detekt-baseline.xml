<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>ComplexCondition:SessionsRepository.kt$SessionsRepository$s.loadingEarlier || s.loadingNewer || !s.hasNewer || s.busy</ID>
    <ID>CyclomaticComplexMethod:SessionsRepository.kt$SessionsRepository$private fun openStream(id: String)</ID>
    <ID>CyclomaticComplexMethod:SessionsRepository.kt$SessionsRepository$suspend fun loadEarlier(id: String)</ID>
    <ID>CyclomaticComplexMethod:SessionsRepository.kt$SessionsRepository$suspend fun loadNewer(id: String)</ID>
    <ID>LongMethod:SessionsRepository.kt$SessionsRepository$private fun openStream(id: String)</ID>
    <ID>LongParameterList:SessionsRepository.kt$SessionsRepository$(id: String, prompt: String, setTitle: Boolean = true, model: String? = null, effort: String? = null, permissionMode: String? = null)</ID>
    <ID>LoopWithTooManyJumpStatements:SessionsRepository.kt$SessionsRepository$while</ID>
    <ID>MagicNumber:FilesRepository.kt$FilesRepository$7</ID>
    <ID>MagicNumber:SessionsRepository.kt$SessionsRepository$10</ID>
    <ID>MagicNumber:SessionsRepository.kt$SessionsRepository$1_000L</ID>
    <ID>MagicNumber:SessionsRepository.kt$SessionsRepository$250</ID>
    <ID>MagicNumber:SessionsRepository.kt$SessionsRepository$2_000L</ID>
    <ID>MagicNumber:SessionsRepository.kt$SessionsRepository$400</ID>
    <ID>MagicNumber:SessionsRepository.kt$SessionsRepository$5_000L</ID>
    <ID>MagicNumber:SessionsRepository.kt$SessionsRepository$8</ID>
    <ID>MagicNumber:WorkflowsRepository.kt$WorkflowsRepository$2_500L</ID>
    <ID>MagicNumber:WorkflowsRepository.kt$WorkflowsRepository$8</ID>
    <ID>MaxLineLength:AuthRepository.kt$AuthRepository$/** Fetch the current FCM token from Firebase and register it with the backend. Best-effort: FCM unavailable in unit tests — exceptions silently swallowed. */</ID>
    <ID>MaxLineLength:AuthRepository.kt$AuthRepository$/** On success the token is in [settings] + [api.token]; [token]/[isLoggedIn] update reactively; FCM is re-registered best-effort (FCM unavailable in unit tests — swallowed). */</ID>
    <ID>MaxLineLength:AuthRepository.kt$AuthRepository$/** Pin a self-signed server cert (TOFU). [hostKey] + [fingerprint] come from [dev.agentic.data.net.AppError.CertUntrusted]. */</ID>
    <ID>MaxLineLength:AuthRepositoryTest.kt$AuthRepositoryTest$// UnconfinedTestDispatcher lets the internal scope.launch run eagerly (no virtual-time advancing needed) without leaking into runTest's uncompleted-coroutine check.</ID>
    <ID>MaxLineLength:ModelCatalog.kt$ModelCatalog$/** Claude-only catalog from GET /api/models?scope=session_start — keeps BYOK provider models out of session-level pickers. */</ID>
    <ID>MaxLineLength:SessionUiStore.kt$SessionUi$/** Per-session workflow-rail UI state. Backed by SharedPreferences under "ui:$id" keys (one small JSON blob per session). */</ID>
    <ID>MaxLineLength:SessionUiStore.kt$SessionUiStore$try { json.decodeFromString&lt;SessionUi&gt;(it) } catch (e: Exception) { AppLog.w("Store", "Failed to decode SessionUi: ${e.message}", e); null }</ID>
    <ID>MaxLineLength:SessionsRepository.kt$/** Min interval between live-stream display() emissions during a burst (see openStream). A turn boundary (ended/busy=false) + the post-stream final flush bypass this so the last state is never throttled away — only high-frequency intermediate text deltas are coalesced. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$EvictedPage$/** Descriptor retained for a page evicted off the NEWEST end (its events ARE dropped to free memory). [upper] is the exclusive `before` cursor to reload with; [count] is how many events it held so the reload is sliced via takeLast — bulletproofing against a reload returning more than the original and overlapping the still-resident newer page. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsLoadState$/** Sealed type for [sessionsStreamWithState]: UI distinguishes first-load failure (server unreachable at launch) from normal updates without changing [sessionsStream]'s shape. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Adopt picker: discover on disk (GET /api/adoptable). Wrapped in [Outcome] so picker VM can render a banner on failure instead of crashing (transient errors fall back to "no candidates"). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** App returned to foreground (wired to ProcessLifecycle ON_START): force every LIVE (non-terminal) session currently ON SCREEN to reconnect. Centralized backstop to per-screen ON_RESUME refresh — heals any live session whose socket went half-open while backgrounded. [restart] is coalesced + identity-guarded, safe to call for several ids and harmless for already-healthy streams. Only sessions with a live UI subscriber (`subscriptionCount &gt; 0`) are reconnected — an idle/off-screen session has no UI to keep fresh and the idle reaper would release it anyway, so reconnecting just churns a WebSocket. Terminal and still-connecting (session == null) flows likewise skipped. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Bare session row (no transcript log) for surfaces that don't need it (e.g. settings sub-page). Distinct from [session]. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Cancel the stream loop and drop all per-id state (job, flow, view, reducer, cursor). Stops the unbounded reconnect loop and lets the maps shrink. Called on kill/delete and from the idle reaper. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Claude-only session-start catalog (GET /api/models?scope=session_start) — BYOK never appears here. Picker then shows only "Default" on error. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Drop all bounded-window state so the next paging rebuilds it from the CURRENT resident events. Called on every reseed (load/post-stream refetch) so a freshly-loaded tail can never be paged over by stale pages. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Evict whole pages off the NEWEST end until resident events ≤ [maxResidentEvents]; push each evicted page's descriptor (newest on top) so [loadNewer] restores them in order. Only RELOAD-SAFE newest pages (≤ PAGE_LIMIT, one server response) are evicted — a rare oversized transient page (accumulated live blob gone terminal before a clean reseed) is kept until the next reseed replaces it; reloading via the 100-capped before=upper would gap. Caller holds [lock]. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Fetch session + persisted log, seed the reducer once, record the cursor, then (if live) open the reconnect loop from `cursor = log.size` so only NEW events stream in — no full backfill. On a failed fetch, clears `connecting` and leaves nodes/session untouched (screen shows error + retry via [reload]). A terminal session opens no stream. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Fetch the model catalog and cache it in [ModelCatalog] so [modelLabel] / [MODEL_OPTIONS] work without a suspend call. Returns entries or empty on error. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Force a fresh transcript reseed AND a fresh live stream for an ALREADY-LOADED session, NOW. Wired to detail screen's ON_RESUME (both layouts) so a warm return rebuilds from the authoritative server log AND reopens the socket via the same proven path a cold start uses. Quiet: load() leaves `connecting` false, so no spinner. No-op if flow not loaded yet — initial load() runs instead. Delegates to [restart], which CANCELS the current stream job before reseeding — the actual fix for the "frozen until force-killed" bug. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Hand a session off to a local Claude Code CLI (POST /api/sessions/:id/detach). The returned `resumeCmd` is the exact command line to paste in a terminal — detail screen displays it copyable next to the "handed off" banner. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Hot, app-scoped transcript state. FIRST call creates the flow (emitting connecting=true) and kicks off initial [load] via [restart]. Repeat calls return the SAME flow instance — that referential reuse IS the cache. Initial load goes through [restart] (not bare `scope.launch { load }`) so it shares the [reconnecting] guard: ON_RESUME refresh + initial load collapse to ONE load rather than two coroutines reseeding the same non-thread-safe reducer. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** In-memory cache first, else the persisted (disk) draft. Disk fallback survives process death (the in-memory map alone dies with the process). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Lazily build the bounded-window page deque for an ENDED session from its current flat resident events (the initial tail load) so paging can start. The tail page's upper bound is the total rendered-line count ([cursorOf], = latestEventId), so before=upper reloads the tail after eviction. Returns null if per-id state is missing. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Like [sessionsStream] but emits [SessionsLoadState] so UI can distinguish first-load failure (server unreachable at launch) from a later blip. First tick success → Loaded forever after; first tick fail → FirstLoadError once, then Loaded with last-good on subsequent ticks (existing keep-last-good preserved). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Load NEWER history (scroll forward) for an ENDED session whose window evicted newer pages. Reloads the most-recently-evicted page via before=&lt;its stored upper&gt;; on an immutable log that returns the exact page. Evicts off the OLDEST end past [maxResidentEvents]. No-op with no evicted newer pages (live sessions never have any). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Load earlier messages (scroll back). LIVE: prepend the page before the oldest cached event and re-seed (unbounded — the stream owns the tail). ENDED: page into the bounded window, evicting whole pages off the NEWEST end past [maxResidentEvents]. Idempotent under concurrency: a single atomic flow.update claim serializes ALL paging (earlier AND newer) so a rapid scroll can't stack duplicate fetches. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Optimistic prompt bridge: list screen writes the just-submitted prompt keyed by new session id; detail reads it to show the prompt before backend log catches up. Thread-safe map. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Per-session UNSENT input draft. SessionViewModel is scoped to the Session nav entry and dies when the user leaves; this app-scoped map persists drafts so re-entering restores them. Cleared on submit + release. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Polls `api.sessions()` every 2s; a failed tick re-emits the last successful list (UI keeps showing sessions across a transient error). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Refresh ONLY [TranscriptState.session] for [id] from the authoritative server row, leaving nodes/busy/ended owned by the stream loop + reducer. Without this, the detail screen's session-derived UI (error banner, repo/model/effort tags, title, input enablement, cost) is frozen at the last load/reseed for the WHOLE duration of a live streaming turn. Discriminates on current state: a clean terminal ("done") is blocked from being downgraded (stale-poll case), but an errored terminal ("failed" with errorKind, or a stopped-by-user "killed") is allowed through (user-initiated recovery: Resume/Retry or a fresh follow-up). Non-terminal→terminal updates are always applied. Failed fetch = no-op (keep-last-good). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Retry the initial transcript load after a first-load failure (a transient server error otherwise leaves the detail screen blank forever — cached flow is never re-run on re-entry). Re-runs [load] on the EXISTING flow so the same cached StateFlow updates in place. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Returns [Outcome] so caller can roll back any optimistic overlay on failure (no reseed needed — live stream delivers the permResolved marker on success). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Send a follow-up turn. On success the returned `since` is where the new turn begins. If NO stream is currently active (e.g. resuming a terminal session whose previous stream closed), open one from `since` so the new turn streams into the same reducer/flow. For a live session the persistent stream is already open and delivers it (no second open). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** Stop the current turn but keep the session live — no [release], so the transcript stream and reducer stay up and the user can immediately send the next message. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** When [idleReleaseMs] is set, watch [id]'s transcript flow and [release] it once it has had ZERO UI subscribers for that long. Detail screen's uiState (a `WhileSubscribed(5s)` combine over `transcript(id)`) is the subscriber, so this fires ~`idleReleaseMs` after the user leaves (or the app is backgrounded past the grace) — never while on screen. collectLatest restarts on flips so a quick navigate-away-and-back cancels the pending release. No-op without a configured timeout (unit tests). */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$/** While collected (detail screen observes its uiState), poll the authoritative session row every 2s and fold it into the cached transcript via [refreshSession]. Reflects REAL current server state within ~2s instead of only at turn end. [pollFlow] + the swallowed fetch error keep it alive across blips. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$AppLog.v("Repo", "load OK id=$id latestEventId=${d.latestEventId} status=${d.session.status} aw=${d.session.awaitingInput}")</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$AppLog.v("Repo", "reaper FIRED id=${id.take(8)} — releasing flow after ${idleMs}ms with no UI subscriber")</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$AppLog.v("Repo", "reconnectLiveSessions: ${live.size}/${synchronized(lock) { flows.size }} live → ${live.map { it.take(8) }}")</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$if (addedEvents.isNotEmpty()) pages.addLast(WindowPage(start = evicted.start, upper = evicted.upper, events = addedEvents))</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$if (reseeded != null) { setCursor(id, d.latestEventId); setEvents(id, d.events); firstEventLines[id] = d.firstEventLine; resetWindow(id) }</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$suspend</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$suspend fun delete(id: String)</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$val d = try { api.sessionEvents(id, INITIAL_LOG_LIMIT) } catch (e: CancellationException) { throw e } catch (e: Exception) { AppLog.w("Repo", "refetch FAILED id=$id"); null }</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$val newSession = (runCatchingOutcome { api.sessionEvents(id, limit = 1) } as? Outcome.Success)?.value?.session ?: return</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$val pages = synchronized(lock) { residentPages[id] } ?: run { flow.update { it.copy(loadingNewer = false) }; return }</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository$val s = (runCatchingOutcome { api.sessionEvents(id, limit = 1) } as? Outcome.Success)?.value?.session</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository.Companion$/** Default warm-keep window for an idle transcript stream. 30s comfortably exceeds the uiState `WhileSubscribed(5s)` grace plus rotation/recomposition blips, so a stream is reaped only once the user has genuinely left the session, never while on screen. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository.Companion$/** Max events a single /events response returns (server caps every request at `min(100)`). A WindowPage built from one such response is reload-safe: before=upper reconstructs it exactly. Eviction skips any oversized page so a transient accumulated blob is never evicted+reloaded with a gap. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$SessionsRepository.Companion$/** Requested `?limit` for initial/reseed /events fetch. NOTE: server clamps EVERY /events request to 100 logical events (`min(100)`, Discord-style), so this does NOT pull 2000; effective seed window is the newest 100, `hasMore` pages the rest via ?before. Sized above the clamp so if server cap is raised the client benefits automatically. Anything older than the seed window is ABSENT until paged in — downstream logic must not assume full history is resident. */</ID>
    <ID>MaxLineLength:SessionsRepository.kt$WindowPage$/** One fetched page of a terminal session's bounded transcript window. [upper] is the exclusive `before` cursor it was fetched with (GET /events?before=upper); re-querying before=upper on an immutable (ended) log returns this exact page — an evicted page reloads byte-identically with no overlap. [start] is the oldest event's line (the page spans [start, upper)). */</ID>
    <ID>MaxLineLength:SessionsRepositoryTest.kt$SessionsRepositoryTest$"""{"type":"assistant","message":{"content":[{"type":"tool_use","name":"Read","input":{"file_path":"/a/B.kt"}}]}}"""</ID>
    <ID>MaxLineLength:SessionsRepositoryTest.kt$SessionsRepositoryTest$Result.success(eventsResponse(runningSession(), events = seedEvents, latestEventId = total.toLong(), firstEventLine = 17L))</ID>
    <ID>MaxLineLength:SessionsRepositoryTest.kt$SessionsRepositoryTest$Result.success(eventsResponse(terminalSession(), events = seedEvents, latestEventId = total.toLong(), firstEventLine = 17L))</ID>
    <ID>MaxLineLength:SessionsRepositoryTest.kt$SessionsRepositoryTest$runCurrent()</ID>
    <ID>MaxLineLength:SessionsRepositoryWindowTest.kt$SessionsRepositoryWindowTest$// Bounded-window tests: paged-log fake serves tail/before windows so we can drive real scroll-back / scroll-forward and assert the resident window stays bounded and tiled (contiguous, no gaps, no duplicates).</ID>
    <ID>MaxLineLength:SettingsStore.kt$// No baked-in default host: first-run path is LAN scan; manual Host field shows placeholder and Log-in stays disabled until typed.</ID>
    <ID>MaxLineLength:WorkflowsRepository.kt$WorkflowsRepository$*</ID>
    <ID>MaxLineLength:WorkflowsRepository.kt$WorkflowsRepository$/** Cold flow that polls `api.outbox(id)` every 2500 ms; SharedFile.mtime is Double (fractional ms from Node.js statSync.mtimeMs), converted with .toLong(). Last-good-on-error same as [runsStream]. */</ID>
    <ID>NestedBlockDepth:SessionsRepository.kt$SessionsRepository$suspend fun followUp(id: String, prompt: String, setTitle: Boolean = true, model: String? = null, effort: String? = null, permissionMode: String? = null): Outcome&lt;Int&gt;</ID>
    <ID>ReturnCount:SessionsRepository.kt$SessionsRepository$private suspend fun load(id: String)</ID>
    <ID>ReturnCount:SessionsRepository.kt$SessionsRepository$suspend fun loadEarlier(id: String)</ID>
    <ID>ReturnCount:SessionsRepository.kt$SessionsRepository$suspend fun loadNewer(id: String)</ID>
    <ID>SwallowedException:SessionsRepository.kt$SessionsRepository$e: Exception</ID>
    <ID>SwallowedException:WorkflowsRepository.kt$WorkflowsRepository$e: Exception</ID>
    <ID>ThrowsCount:SessionsRepository.kt$SessionsRepository$private fun openStream(id: String)</ID>
    <ID>TooGenericExceptionCaught:FilesRepository.kt$FilesRepository$e: Exception</ID>
    <ID>TooGenericExceptionCaught:SessionUiStore.kt$SessionUiStore$e: Exception</ID>
    <ID>TooGenericExceptionCaught:SessionsRepository.kt$SessionsRepository$e: Exception</ID>
    <ID>TooGenericExceptionCaught:WorkflowsRepository.kt$WorkflowsRepository$e: Exception</ID>
    <ID>TooManyFunctions:SessionsRepository.kt$SessionsRepository</ID>
    <ID>UnusedPrivateProperty:SessionsRepositoryTest.kt$SessionsRepositoryTest$// Log entries (buildFromLog shape) that produce a ToolNode and an AnswerNode — used as the // post-stream persisted log so a reseed-on-reconnect reproduces what the live stream delivered. private val toolLogEntry = """{"type":"assistant","message":{"content":[{"type":"tool_use","name":"Read","input":{"file_path":"/a/B.kt"}}]}}"""</ID>
    <ID>UnusedPrivateProperty:SessionsRepositoryTest.kt$SessionsRepositoryTest$private val answerLogEntry = """{"type":"result","result":"done"}"""</ID>
    <ID>UnusedPrivateProperty:WorkflowsRepository.kt$WorkflowsRepository$private val scope: CoroutineScope</ID>
  </CurrentIssues>
</SmellBaseline>
